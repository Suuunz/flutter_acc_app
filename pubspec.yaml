name: aac_app_flutteru
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ✅ 네이버 지도 (최신)
  flutter_naver_map: ^1.4.0

  # ✅ 카카오 로그인 (최신)
  kakao_flutter_sdk: ^1.9.6

  # ✅ http (최신)
  http: ^1.2.0

  # 권한 및 기타 기능
  permission_handler: ^11.0.0
  flutter_sound: ^9.2.13
  path_provider: ^2.1.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true